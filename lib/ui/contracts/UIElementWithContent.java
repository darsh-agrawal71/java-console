package lib.ui.contracts;

/**
 * A base building block of this library that represents a UI element
 * which can't be displayed, but has content of its own.
 */
public interface UIElementWithContent {
    String getContent();
}
